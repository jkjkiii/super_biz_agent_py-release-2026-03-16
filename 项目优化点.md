# SuperBizAgent 项目优化总结

> 一个企业级智能运维 RAG 系统，从「纯向量检索」到「多层记忆 + 混合检索 + 硬路由」的完整演进。


## 一、项目概述

本项目是一个基于 LangGraph + Qwen + Milvus 构建的智能运维助手，经历了从基础 RAG 到完整 Agent 记忆系统的多轮优化，最终形成了一个**意图路由 + 多路召回 + 精排 + 双层记忆**的完整架构。


## 二、优化总览

| 优化模块              | 核心目标               | 关键组件                                   |
| :-------------------- | :--------------------- | :----------------------------------------- |
| **RAG 检索优化**      | 提升召回精度           | 混合检索 (BM25+Dense) + RRF 融合 + 重排序  |
| **意图识别 + 硬路由** | 提升响应速度、降低成本 | IntentRouter + RouteHandlers               |
| **长期记忆**          | 跨会话个性化记忆       | ImportanceScorer + BGE 向量存储 + 时间衰减 |
| **短期记忆**          | 当前会话上下文管理     | 滑动窗口 + 摘要压缩                        |
| **评测体系**          | 量化优化效果           | 传统 IR 指标 + RAGAS                       |

### 优化前后对比

| 维度       | 优化前                 | 优化后                       |
| :--------- | :--------------------- | :--------------------------- |
| 检索策略   | 纯向量检索             | 混合检索 (BM25 + 向量 + RRF) |
| 排序策略   | 直接按向量距离         | 粗召回 + Rerank 精排         |
| 对话路由   | 全部走 Agent           | 意图识别 + 硬路由分流        |
| 记忆能力   | 仅会话内 (MemorySaver) | 长期记忆 + 短期记忆双层      |
| 闲聊处理   | 走 Agent 推理          | 直接 LLM 回复 (无工具)       |
| 数据时效性 | 所有记忆等权           | 时间衰减 (旧数据降权)        |


## 三、模块详解

### 1. RAG 检索优化

#### 1.1 混合检索 (BM25 + Dense)

**为什么**：纯向量检索擅长语义理解，但无法精确匹配错误码、命令、IP 等关键词。

**实现**：

```python
# app/rag/retrievers.py
class HybridRetriever:
    def retrieve(self, query: str):
        # 1. BM25 关键词检索
        bm25_results = sparse_embedding_service.search(query, top_k=50)
        
        # 2. Dense 向量检索
        vector_results = vector_store.similarity_search_with_score(query, k=50)
        
        # 3. RRF 融合
        fused_ids = self._reciprocal_rank_fusion(bm25_results, vector_results)
```

| 对比项     | 纯向量检索 | 混合检索 |
| :--------- | :--------- | :------- |
| 语义理解   | ✅ 好       | ✅ 好     |
| 精确匹配   | ❌ 差       | ✅ 好     |
| 召回稳定性 | 中         | 高       |

#### 1.2 RRF (倒数排名融合)

**为什么**：BM25 分数 (0~∞) 和向量距离 (0~1) 量纲不同，无法直接加权平均。

**公式**：

```python
RRF_score(doc) = Σ 1 / (k + rank_i)  # k=60, rank_i 是 doc 在第 i 个系统中的排名
```

**效果**：只看排名不看分数，公平融合两路召回结果。

#### 1.3 重排序 (Rerank)

**为什么**：向量检索是双塔模型，Q 和 D 没有交互；Cross-Encoder 做深度融合，排序更准。

**实现**：

```python
# app/services/reranker_service.py
class RerankerService:
    def load_model(self):
        self.model = CrossEncoder("BAAI/bge-reranker-v2-m3")
    
    def rerank(self, query, documents, top_k=5):
        pairs = [(query, doc.page_content) for doc in documents]
        scores = self.model.predict(pairs)
        return sorted(zip(scores, documents), reverse=True)[:top_k]
```

**效果对比**：

| 场景          | 纯向量      | 重排序后                       |
| :------------ | :---------- | :----------------------------- |
| 问答匹配精度  | 一般        | 显著提升                       |
| 处理否定/指代 | 弱          | 强                             |
| 响应速度      | 快 (毫秒级) | 慢 (秒级) → 只对 30 个候选精排 |


### 2. 意图识别 + 硬路由

#### 2.1 整体架构

```
用户请求 → 意图识别 → 硬路由分流 → 场景处理器 → 返回
                │
                ├─ 闲聊 → 直接 LLM (不检索)
                ├─ 知识查询 → 快速 RAG
                ├─ 故障排查 → 日志 + 监控 + RAG
                └─ 系统控制 → 模拟/执行操作
```

#### 2.2 意图识别实现

**双层匹配**：

| 层级      | 方式                | 响应速度 | 覆盖率 |
| :-------- | :------------------ | :------- | :----- |
| Fast Path | 关键词匹配          | 毫秒级   | 90%    |
| Slow Path | LLM 分类 (qwen-max) | 秒级     | 10%    |

```python
# app/core/intent_router.py
def _fast_match(self, query: str):
    # 关键词 → 意图映射
    if "CPU" in query and "高" in query:
        return Intent.TROUBLESHOOT
    # ... 更多规则

async def _llm_classify(self, query: str):
    # 大模型精准分类
    return await chain.ainvoke({"user_input": query})
```

#### 2.3 场景处理器

| 意图         | 处理链路              | 特点                 |
| :----------- | :-------------------- | :------------------- |
| **闲聊**     | 直接 LLM，不检索      | 响应 < 1s，零成本    |
| **知识查询** | 混合检索 + LLM        | 跳过 Agent，快速应答 |
| **故障排查** | MCP 日志 + 监控 + RAG | 工具组合，全面诊断   |
| **系统控制** | 执行 K8s/云 API       | 直接操作             |

#### 2.4 优化效果

| 场景            | 优化前         | 优化后       |
| :-------------- | :------------- | :----------- |
| 闲聊响应时间    | ~3s (走 Agent) | ~0.5s (直出) |
| 闲聊 Token 消耗 | 2500+          | < 100        |


### 3. 长期记忆 (BGE 向量记忆)

#### 3.1 架构设计

```
用户对话 → 重要性打分 → BGE 向量化 → Milvus 存储 → 语义检索召回 → Prompt 注入
              ↓
          阈值 0.7 过滤噪音
```

#### 3.2 核心机制

##### ① 重要性阈值

**为什么**：防止"你好""谢谢"污染长期记忆库，保证召回质量。

```python
# app/memory/importance_scorer.py
def _rule_based_score(self, content: str):
    score = 0.0
    if re.search(r'\d+\.\d+\.\d+\.\d+', content):  # IP
        score += 0.35
    if re.search(r'\b\d{4,}\b', content):          # 错误码
        score += 0.15
    if '配置' in content or '设置' in content:     # 配置关键词
        score += 0.1
    return score  # 0.7 以上才存入长期记忆
```

**双模式打分**：

| 模式     | 速度   | 准确度 | 触发条件         |
| :------- | :----- | :----- | :--------------- |
| 规则打分 | 毫秒级 | 一般   | 所有消息         |
| LLM 精修 | 秒级   | 高     | 规则分数 0.2~0.8 |

##### ② BGE 语义向量存储

**为什么选 BGE**：MTEB 中文榜单领先，语义理解能力强，支持中英文混杂。

```python
# 存储格式
{
    "id": "mem_xxx",
    "vector": [0.1, 0.2, ...],  # 1024维
    "content": "服务器 IP 是 192.168.1.100",
    "metadata": {
        "user_id": "default_user",
        "importance": 0.70,
        "timestamp": "2026-09-08T10:00:00"
    }
}
```

##### ③ 用户隔离 (user_id)

| 对比项     | session_id 隔离 | user_id 隔离 |
| :--------- | :-------------- | :----------- |
| 新建会话   | ❌ 记忆丢失      | ✅ 记忆保留   |
| 换设备登录 | ❌ 查不到        | ✅ 能查到     |

##### ④ 时间衰减

**为什么**：旧数据可能失效，应该自然降权而非物理删除。

**公式**：`有效权重 = 原始重要性 × e^(-0.01 × 天数)`

| 天数 | 权重 | 含义             |
| :--- | :--- | :--------------- |
| 0    | 0.70 | 刚存入           |
| 30   | 0.52 | 一个月后降权 26% |
| 60   | 0.38 | 两个月后减半     |
| 180  | 0.12 | 半年后几乎不召回 |

**设计原则**：「用户数据是宝贵资产，绝不物理删除」 → 检索时动态计算衰减，而非 TTL 物理删除。


### 4. 短期记忆 (滑动窗口 + 摘要压缩)

#### 4.1 架构

```
用户消息 → 滑动窗口 (最近 10 条) → Prompt 注入
               ↓ (窗口满了)
          摘要压缩 (异步 LLM) → 摘要放回窗口
```

#### 4.2 两种策略对比

| 对比项                   | 暴力截断 | 摘要压缩            |
| :----------------------- | :------- | :------------------ |
| 第 1 轮说 IP，第 11 轮问 | ❌ 丢失   | ✅ 摘要中保留        |
| Token 消耗               | 固定     | 固定 + 少量压缩成本 |
| 实现复杂度               | 低       | 中                  |

#### 4.3 压缩原则

```
1. 保留关键事实：错误码、IP、服务名、版本号
2. 保留问题经过：发生了什么、尝试了什么
3. 忽略日常问候：你好、谢谢、好的
```

#### 4.4 异步执行

```python
# 窗口满时立即返回，后台异步压缩
def _on_window_overflow(self, old_messages):
    asyncio.create_task(self._async_summarize(old_messages))  # 不阻塞用户
```


### 5. 评测体系

#### 5.1 传统 IR 指标 (检索质量)

| 指标     | 含义                            | 用于评估     |
| :------- | :------------------------------ | :----------- |
| Recall@K | 前 K 个结果包含多少相关文档     | 召回能力     |
| MRR      | 第一个正确答案的排名            | 头位质量     |
| NDCG@K   | 排序质量 (考虑位置和相关性分级) | 整体排序     |
| MAP      | 平均精度均值                    | 综合检索质量 |

#### 5.2 RAGAS (生成质量)

| 指标              | 含义                                  |
| :---------------- | :------------------------------------ |
| Faithfulness      | 答案是否忠实于检索到的上下文 (防幻觉) |
| Answer Relevancy  | 回答是否切题                          |
| Context Precision | 检索到的文档相关度排序                |
| Context Recall    | 标准答案中的信息点是否被覆盖          |

#### 5.3 离线测试流程

```bash
# 1. 准备测试数据 (UUID 版本)
python prepare_testset.py

# 2. 运行 RAG 系统生成答案
python run_rag_system.py

# 3. RAGAS 评估
python evaluate_with_ragas.py
```


## 四、技术栈

| 组件       | 技术选型                | 说明                              |
| :--------- | :---------------------- | :-------------------------------- |
| LLM        | Qwen-Max                | 阿里千问，通过 LangChain 集成     |
| Embedding  | text-embedding-v4       | BGE 系列，1024 维                 |
| 向量数据库 | Milvus                  | 2.x 版本，支持向量 + JSON 过滤    |
| Agent 框架 | LangGraph               | 工具调用、会话管理、流式输出      |
| 重排序     | BAAI/bge-reranker-v2-m3 | Cross-Encoder 精排                |
| BM25       | rank-bm25 + jieba       | 中文分词关键词检索                |
| RAG 评估   | RAGAS                   | Faithfulness、Answer Relevancy 等 |
| API 框架   | FastAPI + SSE           | 流式对话支持                      |


## 五、系统架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         API 层 (FastAPI)                           │
│                    /api/chat (流式/非流式)                         │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      意图识别层 (IntentRouter)                      │
│                  关键词匹配 (Fast) + LLM 分类 (Slow)                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      硬路由层 (RouteHandlers)                       │
│       ┌──────────┬──────────┬──────────┬──────────┐                │
│       │  闲聊    │  知识查询 │  故障排查 │  系统控制 │                │
│       │  直接LLM │  快速RAG │  工具组合 │  执行操作 │                │
│       └──────────┴──────────┴──────────┴──────────┘                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      检索层 (HybridRetriever)                       │
│              BM25 + Dense Vector + RRF 融合 + Rerank               │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      记忆系统层 (Memory System)                     │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐  │
│  │      短期记忆 (Short-term)   │  │      长期记忆 (Long-term)    │  │
│  │     滑动窗口 + 摘要压缩      │  │   重要性阈值 + BGE + 时间衰减 │  │
│  │      内存 (deque)           │  │      Milvus (持久化)         │  │
│  └─────────────────────────────┘  └─────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      基础设施层 (Infrastructure)                    │
│              Milvus  │  MCP 工具  │  DashScope  │  Config         │
└─────────────────────────────────────────────────────────────────────┘
```


## 六、项目亮点 (面试/展示用)

1. **混合检索**：BM25 精确匹配 + BGE 语义理解，双路召回 + RRF 融合 + Cross-Encoder 重排序

2. **意图路由**：关键词快速匹配 + LLM 精准分类，闲聊直出节省 95% Token 消耗

3. **双层记忆**：
   - 短期：滑动窗口 + 摘要压缩，用 10% Token 保留 90% 有效信息
   - 长期：重要性阈值 + BGE 向量 + 时间衰减，跨会话个性化记忆

4. **数据安全**：长期记忆永不物理删除，仅通过时间衰减自然降权

5. **评测体系**：传统 IR 指标 (Recall、MRR、NDCG) + RAGAS (Faithfulness、Answer Relevancy) 双轨评估

6. **工程实践**：
   - 完整会话管理 (MemorySaver)
   - 流式/非流式双模式
   - 软硬路由切换
   - 异步摘要压缩 (不阻塞用户)
   - 降级方案 (LLM 失败时规则兜底)


## 七、目录结构

```
app/
├── api/                     # API 接口 (chat, upload)
├── core/                    # 核心组件
│   ├── intent_router.py     # 意图识别
│   └── milvus_client.py     # Milvus 客户端
├── services/                # 业务服务
│   ├── rag_agent_service.py # Agent 主服务
│   ├── vector_store_manager.py
│   ├── vector_embedding_service.py
│   ├── reranker_service.py  # 重排序
│   └── sparse_embedding_service.py # BM25
├── rag/                     # RAG 策略
│   ├── retrievers.py        # 混合检索器
│   └── pipelines.py         # 流程编排
├── memory/                  # 记忆系统
│   ├── long_term.py         # 长期记忆 (BGE+Milvus)
│   ├── short_term.py        # 短期记忆 (滑动窗口)
│   ├── summarizer.py        # 摘要压缩
│   └── importance_scorer.py # 重要性打分
├── evaluation/              # 评测
│   ├── metrics_calculator.py
│   ├── prepare_testset.py
│   ├── run_rag_system.py
│   └── evaluate_with_ragas.py
├── tools/                   # 工具定义
├── agent/                   # MCP 客户端
└── models/                  # 数据模型
```

---

> 项目地址：[GitHub 仓库链接]  
> 版本：v2.0  
> 最后更新：2026 年 9 月